## 指令速查（当前会话执行）

- `dotnet add src/MudBlazorLab.Web/MudBlazorLab.Web.csproj package Bogus`
  - 作用：安装 `Bogus` 包，用于生成大量模拟数据，便于性能与联动测试
  - 使用场景：需要快速构造 10k+ 规模的测试数据集

- `dotnet build`
  - 作用：常规构建解决方案或项目，输出完整构建信息
  - 使用场景：本地开发的标准构建

- `dotnet build -nologo -clp:errorsonly`
  - 作用：仅输出错误信息，隐藏 Logo 与冗余日志，快速定位问题
  - 使用场景：频繁迭代时的快速验证（本会话用于每次代码编辑后的验证）

> 注：以上为本会话内实际执行的指令与用途说明；后续若新增其它指令，我会继续补充。

