M5 扩展注册 UI 原型

目标与路由
- 路由：`/factory/extensions`
- 目标：注册外部策略/设备/配方，安全沙箱与版本治理。

数据源与契约
- `POST /api/extensions/register` → `{ id, status }`

布局与交互
- 注册表单：类型、名称、版本、载荷（JSON），校验结果与签名信息。
- 注册记录与状态；启用/禁用开关；运行日志与错误捕获。

验收标准
- 注册与校验通过；异常与配额限制生效；不会破坏守恒与非负性。

