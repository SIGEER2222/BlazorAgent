M5 报告导出 UI 原型

目标与路由
- 路由：`/factory/reports/export`
- 目标：导出吞吐/能耗/库存等报表，包含元数据与窗口聚合。

数据源与契约
- `GET /api/reports/export?type=excel` → 文件下载

布局与交互
- 导出参数选择：类型（CSV/Excel）、时间范围、窗口大小、维度（throughput/energy/inventory/wip/cost）。
- 预览与字段映射说明；导出记录列表。

验收标准
- 导出字段完整且格式正确；包含版本与采样频率元数据。

