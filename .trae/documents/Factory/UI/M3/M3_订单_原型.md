M3 订单管理 UI 原型

目标与路由
- 路由：`/factory/orders`
- 目标：创建与管理订单，追踪状态与关联的方案。

数据源与契约
- `POST /api/orders` → `{ id, status }`
- `GET /api/orders/{id}` → `Order`

布局与交互
- 列表页：订单编号、目标物料与数量、交期、优先级、状态。
- 详情抽屉：订单项明细、关联方案链接、生成与仿真入口。
- 操作：创建订单、调整优先级、查看关联方案与执行进度。

验收标准
- 订单创建与查询正确；与规划页面联动。

